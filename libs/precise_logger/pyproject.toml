[project]
name = "precise-logger"
version = "0.1.0"
readme = "README.md"
authors = [{ name = "Di-Is", email = "rohoxbox@gmail.com" }]
requires-python = "~=3.9"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
test = ["pytest"]
