[project]
name = "lib"
version = "0.1.0"
requires-python = "~=3.9"
dependencies = [
    "lib-core",
    "numpy>=2.0.2",
    "pandas>=2.2.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
lib-core = { path = "../lib-core", editable = true }